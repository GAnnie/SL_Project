// For saving splat map as PNG file. 
//RUN THIS SCRIPT BY SELECTING TERRAIN > EXPORT TEXTURE in the main menu.
//Script Found HERE: http://answers.unity3d.com/questions/13479/exporting-terrain-splatmap.html
import System.IO; 

@MenuItem("Terrain/Export Texture") 
static function Apply () { 
//   var texture : Texture2D = Selection.activeObject as Texture2D; 
//   if (texture == null) 
//   { 
//      EditorUtility.DisplayDialog("Select Texture", "You Must Select a Texture first!", "Ok"); 
//      return; 
//   } 
//
//	var exportTexutre : Texture2D = new Texture2D( terrainData.alphamapWidth, terrainData.alphamapHeight,TextureFormat., false );
//
//
//   var bytes = texture.EncodeToPNG(); 
//   File.WriteAllBytes(Application.dataPath + "/exported_texture.png", bytes); 
} 