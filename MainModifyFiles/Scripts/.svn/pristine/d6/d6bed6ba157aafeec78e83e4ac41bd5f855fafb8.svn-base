using UnityEngine;
using System.Collections;

public class TestVersion : MonoBehaviour
{
	void Start () 
	{
		if (Version.Release)
		{
			UILabel label = this.gameObject.GetComponent<UILabel>();
			label.text = "此游戏处于研发阶段\n现在版本不代表最终品质";
		}
	}
}